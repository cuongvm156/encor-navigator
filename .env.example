# No environment variables are required for the audio smoke test.
#
# The demo audio source is a single canonical URL defined in
# src/config/demoAudio.ts so desktop and mobile previews always resolve the
# exact same file. VITE_DEMO_AUDIO_URL is no longer read.
