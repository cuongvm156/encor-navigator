# Optional development / smoke-test audio source.
#
# Used ONLY as a fallback when the selected chapter has no valid real audio URL
# in the course data. Public client configuration — NOT a secret.
# No copyrighted training audio is committed to this repository.
VITE_DEMO_AUDIO_URL=https://example.com/test-audio.mp3
